This project is a Blog API using the full set of Django REST Framework features. 
It has users, permissions, and a full CRUD (Create-Read-Update-Delete) functionality. 
Viewsets and routers are used.


